/**
 * Back-compat re-export. The real client lives in `src/api/axiosClient.ts`.
 * New code should import directly from `@/api/axiosClient`.
 */
import axiosClient from "@/api/axiosClient";
export default axiosClient;
